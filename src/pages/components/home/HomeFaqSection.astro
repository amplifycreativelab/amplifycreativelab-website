---
const faqs = [
  {
    question: "How much do your photography sessions cost?",
    answer:
      "Every project is a little different, but we offer clear, upfront pricing based on the type of shoot (food, product or event), time required and deliverables. Tell us a bit about your project and we’ll send you a custom quote with no surprises.",
  },
  {
    question: "Do you travel to restaurants, venues or client locations?",
    answer:
      "Yes. We regularly shoot on location at restaurants, cafés, commercial kitchens, studios and event venues around Perth. Travel within the Perth metro area is usually included—just share your location when you enquire.",
  },
  {
    question: "How long does a typical photo session take?",
    answer:
      "For food and product photography, most sessions run between 2–4 hours depending on the number of dishes or items. Event coverage usually starts from 2 hours and can be extended as needed.",
  },
  {
    question: "When will I receive my edited photos?",
    answer:
      "For most projects, you’ll receive your edited images within 7–10 business days. If you have a specific launch date or campaign deadline, let us know and we’ll do our best to prioritise your project.",
  },
  {
    question: "Do you provide both RAW and edited images?",
    answer:
      "We deliver carefully edited, high-resolution images ready for web, social and print. RAW files are not included by default, but can be provided for certain commercial projects on request.",
  },
  {
    question: "How do I book a session?",
    answer:
      "Simply send us an enquiry through the contact form with a few details about your project (type of shoot, location, preferred dates), and we’ll get back to you with availability, pricing and next steps.",
  },
];

// This logic limits the list to the first 4 items
const topFaqs = faqs.slice(0, 4);
---

<section class="faq-section" aria-labelledby="faq-heading">

<section class="faq-section" aria-labelledby="faq-heading">
  <div class="faq-inner">
    
    <div class="faq-column faq-column--intro">
      <h2 id="faq-heading" class="faq-title">FAQ</h2>
      <p class="faq-subtitle">Common questions about our process.</p>
      
      <a href="/faq" class="faq-link">
        View all questions &rarr;
      </a>
    </div>

    <div class="faq-column faq-column--questions">
      <div class="faq-list">
        {
          topFaqs.map((item, index) => (
            <details class="faq-item" {...(index === 0 ? { open: true } : {})}>
              <summary class="faq-question">
                <span>{item.question}</span>
                <span class="faq-icon" aria-hidden="true">+</span>
              </summary>
              <div class="faq-answer-wrapper">
                <p class="faq-answer">{item.answer}</p>
              </div>
            </details>
          ))
        }
      </div>
    </div>
  </div>
</section>

<style>
  /* Same styles as Option 1, with these additions/tweaks */

  .faq-section {
    --color-bg: #fef8eb;
    --color-text: #93897e;
    --color-heading: #152705;
    --color-border: rgba(147, 137, 126, 0.3);

    background-color: var(--color-bg);
    color: var(--color-text);
    padding: clamp(4rem, 8vw, 6rem) clamp(1.5rem, 4vw, 3rem);
  }

  .faq-inner {
    max-width: 1400px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 2fr; 
    gap: clamp(3rem, 5vw, 6rem);
    align-items: start; 
  }

  .faq-column--intro {
    position: sticky;
    top: 2rem; 
    padding-top: 1rem;
    border-top: 1px solid var(--color-heading); 
    display: flex;
    flex-direction: column;
    align-items: flex-start; /* Aligns button to left */
  }

  .faq-title {
    color: var(--color-heading);
    font-size: clamp(3rem, 5vw, 4.5rem); 
    letter-spacing: -0.02em;
    line-height: 1;
    margin-bottom: 1rem;
    font-family: "Abril Fatface", serif;
  }

  .faq-subtitle {
    font-size: 0.9rem;
    font-family: sans-serif;
    margin-bottom: 2rem;
    opacity: 0.8;
  }

  /* NEW BUTTON STYLE */
  .faq-link {
    display: inline-block;
    text-decoration: none;
    font-size: 0.9rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-heading);
    border-bottom: 1px solid currentColor;
    padding-bottom: 2px;
    transition: opacity 0.2s;
  }
  
  .faq-link:hover {
    opacity: 0.6;
  }

  .faq-list {
    display: flex;
    flex-direction: column;
    width: 100%;
  }

  .faq-item {
    border-bottom: 1px solid var(--color-border);
    padding: 1.5rem 0;
  }
  
  .faq-item:first-child {
    padding-top: 0; 
  }

  .faq-question {
    list-style: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-size: 1.1rem;
    font-weight: 500;
    color: var(--color-heading);
    transition: color 0.2s;
  }

  .faq-question:hover {
    color: #5e564d;
  }

  .faq-question::-webkit-details-marker {
    display: none;
  }

  .faq-icon {
    font-size: 1.5rem;
    font-weight: 300;
    transition: transform 0.3s ease;
  }

  .faq-item[open] .faq-icon {
    transform: rotate(45deg);
  }

  .faq-answer {
    margin-top: 1rem;
    font-size: 0.95rem;
    line-height: 1.7;
    max-width: 65ch;
    color: var(--color-text);
  }

  /* Mobile Responsive */
  @media (max-width: 900px) {
    .faq-inner {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
    .faq-column--intro {
      position: relative;
      top: 0;
      border-top: none;
      align-items: center; /* Center align on mobile */
      text-align: center;
    }
    
    /* Hide the button on mobile if you want to save space, 
       otherwise keep it */
    .faq-subtitle {
        margin-bottom: 1.5rem;
    }
  }
</style>