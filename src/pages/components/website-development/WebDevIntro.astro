---
import { Image } from "astro:assets";

const title = "Website Design & Development";
---

<section class="webdev-intro">
  <div class="webdev-intro__inner">
    <div class="webdev-intro__content">
      <p class="webdev-intro__kicker">FOR RESTAURANTS, CAFÉS & FOOD BRANDS</p>
      <h1 class="webdev-intro__title">{title}</h1>
      <p class="webdev-intro__lead">
        Your website should work as hard as your best front-of-house team member:
        welcoming, clear, and always nudging guests to book or buy.
      </p>
      <p class="webdev-intro__body">
        We design and build fast, search-friendly websites that showcase your food
        photography, tell your brand story and make it effortless for people to
        reserve a table.
      </p>
      <a href="#web-pricing" class="webdev-intro__cta">View Packages</a>
    </div>
    
    <div class="webdev-intro__visual">
      <Image 
        src="/images/portfolio/portrait/corporate-business-portrait-man-laptop-perth.webp" 
        alt="Mobile website design for a restaurant" 
        width="600" 
        height="800"
        loading="eager"
      />
    </div>
  </div>
</section>

<style>
  .webdev-intro {
    background: #152705;
    color: #fef8eb;
    padding: clamp(3rem, 6vw, 5.5rem) clamp(1.5rem, 6vw, 4rem);
    position: relative;
    overflow: hidden;
  }

  /* Grain Texture Overlay */
  .webdev-intro::before {
    content: "";
    position: absolute;
    top: 0; left: 0; width: 100%; height: 100%;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)' opacity='0.05'/%3E%3C/svg%3E");
    pointer-events: none;
    mix-blend-mode: overlay;
    opacity: 0.4;
    z-index: 0;
  }

  .webdev-intro__inner {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: clamp(3rem, 5vw, 5rem);
    align-items: center;
    position: relative;
    z-index: 1;
  }

  .webdev-intro__content {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
  }

  .webdev-intro__kicker {
    font-family: "Montserrat", system-ui, sans-serif;
    font-size: 0.8rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: #fadab8; /* Highlight color for kicker */
    margin-bottom: 1rem;
  }

  .webdev-intro__title {
    font-family: "Abril Fatface", system-ui, serif;
    font-size: clamp(2.5rem, 5vw, 3.8rem);
    line-height: 1.05;
    margin: 0 0 1.5rem;
  }

  .webdev-intro__lead {
    font-family: "Darker Grotesque", system-ui, sans-serif;
    font-size: 1.35rem;
    line-height: 1.4;
    max-width: 40rem;
    margin: 0 0 1rem;
    font-weight: 500;
  }

  .webdev-intro__body {
    font-family: "Darker Grotesque", system-ui, sans-serif;
    font-size: 1.1rem;
    line-height: 1.6;
    max-width: 38rem;
    margin: 0 0 2rem;
    opacity: 0.85;
  }

  .webdev-intro__cta {
    display: inline-block;
    background: #fadab8;
    color: #152705;
    padding: 0.8rem 1.8rem;
    border-radius: 999px;
    text-decoration: none;
    font-family: "Montserrat", system-ui, sans-serif;
    font-weight: 600;
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    transition: transform 0.2s ease, background-color 0.2s ease;
  }

  .webdev-intro__cta:hover {
    transform: translateY(-2px);
    background: #fff;
  }

  /* Image styling */
  .webdev-intro__visual img {
    width: 100%;
    height: auto;
    border-radius: 12px;
    box-shadow: 0 20px 50px rgba(0,0,0,0.4);
    transform: rotate(2deg); /* Editorial tilt */
    border: 1px solid rgba(255,255,255,0.1);
  }

  @media (max-width: 900px) {
    .webdev-intro__inner {
      grid-template-columns: 1fr;
      text-align: left;
    }
    
    .webdev-intro__visual {
      order: -1; /* Image on top for mobile */
      max-width: 500px;
      margin: 0 auto;
    }
    
    .webdev-intro__visual img {
      transform: rotate(0deg);
    }
  }

  @media (max-width: 768px) {
    .webdev-intro {
      padding-inline: 1.25rem;
    }
  }
</style>
