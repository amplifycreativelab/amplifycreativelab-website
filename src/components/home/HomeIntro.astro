---

---

<div class="intro">
  <div class="intro-inner">
    <h1 class="intro-title">
      Amplify Creative Lab is a Perth creative studio specialising in
      commercial, food and product photography, and custom website design for
      hospitality, retail and e-commerce brands.
    </h1>
  </div>

  <style>
    .intro {
      width: 100vw;

      min-height: 20vh;

      display: flex;

      flex-direction: column;

      justify-content: center;

      align-items: center;

      background: transparent;
    }

    /* keeps content from stretching too wide, like Elementor content-width */
    .intro-inner {
      margin: 0;
      padding: 10px 50px;

      text-align: center;
    }

    .intro-title {
      font-family: var(--font-heading);

      font-size: clamp(1.5rem, 3vw, 3rem);
      font-weight: 100;
      line-height: normal;
      color: var(--color-heading);
      /* was #152705 */
      margin: 0;
    }

    /* Mobile spacing */
    @media (max-width: 768px) {
      .intro-inner {
        padding: 30px 30px;
        /* was 40px 20px */
      }

      .intro-title {
        text-align: center;
      }
    }
  </style>
</div>
