---
import { Image } from "astro:assets";
import stefanoImage from "../../assets/images/about/stefano-web-developer-astro-expert-perth.webp";

const title = "Website Design & Development";
---

<section class="webdev-intro">
  <div class="webdev-intro__inner">
    <div class="webdev-intro__content">
      <div class="webdev-intro__kicker-wrapper">
        <span class="webdev-intro__line"></span>
        <p class="webdev-intro__kicker">
          FOR BUSINESSES, BRANDS & SOLE TRADERS
        </p>
      </div>

      <h1 class="webdev-intro__title">{title}</h1>

      <p class="webdev-intro__lead">
        As a Perth website designer and developer, we build fast,
        search-friendly websites that feel as welcoming as your best
        front-of-house team member—clear, intentional and built to convert.
      </p>

      <p class="webdev-intro__body">
        We build high-converting websites for service businesses, sole traders,
        lifestyle brands and hospitality venues in Perth. Every site is designed
        to showcase your brand, tell your story and make it effortless for
        visitors to book, buy or enquire. We can also handle tailored
        <a href="/perth-food-photography/">Photography</a> and content creation to
        keep your visuals and website perfectly aligned.
      </p>

      <a href="#web-pricing" class="webdev-intro__cta">View Packages</a>
    </div>

    <div class="webdev-intro__visual">
      <Image
        src={stefanoImage}
        alt="Stefano, lead web developer in Perth holding a laptop, specializing in high-performance Astro and custom-coded websites."
        width="600"
        height="800"
        loading="eager"
        class="hero-image"
      />
    </div>
  </div>
</section>

<style>
  .webdev-intro {
    /* Richer background with radial spotlight */
    background: radial-gradient(circle at 80% 20%, #2a4410 0%, #152705 60%);
    color: #fef8eb;
    padding: clamp(4rem, 8vw, 8rem) clamp(1.5rem, 6vw, 4rem);
    position: relative;
    overflow: hidden;
  }

  /* Grain Texture Overlay */
  .webdev-intro::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)' opacity='0.05'/%3E%3C/svg%3E");
    pointer-events: none;
    mix-blend-mode: overlay;
    opacity: 0.3;
    z-index: 0;
  }

  .webdev-intro__inner {
    max-width: 1300px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(3rem, 6vw, 8rem);
    align-items: center;
    position: relative;
    z-index: 1;
  }

  .webdev-intro__content {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
  }

  /* Kicker with decorative line */
  .webdev-intro__kicker-wrapper {
    display: flex;
    align-items: center;
    gap: 15px;
    margin-bottom: 2rem;
  }

  .webdev-intro__line {
    width: 60px;
    height: 1px;
    background-color: #fadab8;
    opacity: 0.6;
  }

  .webdev-intro__kicker {
    font-family: "Montserrat", system-ui, sans-serif;
    font-size: 0.85rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: #fadab8;
    margin: 0;
    font-weight: 500;
  }

  .webdev-intro__title {
    font-family: "Abril Fatface", system-ui, serif;
    font-size: clamp(3.5rem, 7vw, 5.5rem);
    line-height: 1.05;
    margin: 0 0 2.5rem;
    position: relative;
  }

  /* Decorative hanging line for paragraphs */
  .webdev-intro__lead {
    font-family: "Darker Grotesque", system-ui, sans-serif;
    font-size: 1.6rem;
    line-height: 1.3;
    max-width: 38rem;
    margin: 0 0 1.5rem;
    font-weight: 500;
    color: #fff;
    padding-left: 1.5rem;
    border-left: 1px solid rgba(250, 218, 184, 0.3);
  }

  .webdev-intro__body {
    font-family: "Darker Grotesque", system-ui, sans-serif;
    font-size: 1.25rem;
    line-height: 1.6;
    max-width: 36rem;
    margin: 0 0 3rem;
    padding-left: 1.5rem; /* Align with lead */
    opacity: 0.9;
    color: #e3decb;
    border-left: 1px solid transparent; /* Keeps alignment */
  }

  .webdev-intro__body a {
    color: #fadab8;
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 4px;
    transition: color 0.2s;
  }

  .webdev-intro__body a:hover {
    color: #fff;
  }

  .webdev-intro__cta {
    margin-left: 1.5rem; /* Align with text block */
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    background: transparent;
    color: #fadab8;
    padding: 1rem 2.5rem;
    border: 1px solid rgba(250, 218, 184, 0.3);
    border-radius: 999px;
    text-decoration: none;
    font-family: "Montserrat", system-ui, sans-serif;
    font-weight: 600;
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    transition: all 0.3s ease;
  }

  .webdev-intro__cta:hover {
    background: #fadab8;
    color: #152705;
    border-color: #fadab8;
    transform: translateY(-2px);
  }

  /* --- ARCH VISUAL CONTAINER --- */
  .webdev-intro__visual {
    position: relative;
    /* Create the Arch Shape on the container */
    border-radius: 300px 300px 20px 20px;
    overflow: hidden;
    aspect-ratio: 3/4;
    width: 100%;
    /* Strong gold border to make the shape pop */
    border: 1px solid #fadab8;
    /* Deep shadow for depth */
    box-shadow: 0 30px 60px -10px rgba(0, 0, 0, 0.6);
    transform: translateZ(0);
    opacity: 0;
    animation: fadeUp 1s ease-out forwards;
    animation-delay: 0.2s;
  }

  .webdev-intro__visual img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 1.5s cubic-bezier(0.25, 1, 0.5, 1);
  }

  /* Parallax/Zoom effect on hover */
  .webdev-intro__visual:hover img {
    transform: scale(1.05);
  }

  @keyframes fadeUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 1100px) {
    .webdev-intro__title {
      font-size: clamp(3rem, 6vw, 4.5rem);
    }
  }

  @media (max-width: 900px) {
    .webdev-intro__inner {
      grid-template-columns: 1fr;
      text-align: left;
      gap: 3rem;
    }

    .webdev-intro__visual {
      order: -1;
      max-height: 600px;
      margin: 0 auto;
      max-width: 500px;
    }
  }

  @media (max-width: 768px) {
    .webdev-intro {
      padding-inline: 1.25rem;
    }
    .webdev-intro__lead,
    .webdev-intro__body {
      padding-left: 0;
      border-left: none;
      margin-left: 0;
    }
    .webdev-intro__line {
      display: none; /* Hide decorative line on mobile */
    }
    .webdev-intro__kicker-wrapper {
      margin-bottom: 1rem;
    }
    .webdev-intro__cta {
      border-left: none;
      margin-left: 0;
    }
  }
</style>
