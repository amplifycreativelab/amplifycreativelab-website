<section class="webdev-final-cta" aria-label="Website development enquiry">
  <div class="webdev-final-cta__inner">
    <div class="webdev-final-cta__card">
      <p class="webdev-final-cta__kicker">READY TO START?</p>
      <h2 class="webdev-final-cta__title">Let&apos;s build your Perth website</h2>
      <p class="webdev-final-cta__text">
        Share your business name, what you offer, and your goal (leads, bookings
        or sales). We&apos;ll recommend the best package and next steps.
      </p>

      <div class="webdev-final-cta__actions">
        <a class="webdev-final-cta__btn webdev-final-cta__btn--primary" href="/contact/"
          >Get a quote</a
        >
        <a class="webdev-final-cta__btn webdev-final-cta__btn--secondary" href="#web-pricing"
          >View packages</a
        >
      </div>
    </div>
  </div>
</section>

<style>
  .webdev-final-cta {
    background-color: #fef8eb;
    padding: clamp(3.5rem, 6vw, 5rem) 24px;
    border-top: 1px solid #e3d4b4;
  }

  .webdev-final-cta__inner {
    max-width: 1100px;
    margin: 0 auto;
  }

  .webdev-final-cta__card {
    border-radius: 18px;
    background: radial-gradient(circle at 20% 10%, #24360f 0%, #152705 70%);
    border: 1px solid rgba(250, 218, 184, 0.28);
    padding: clamp(1.75rem, 4vw, 3rem);
    text-align: center;
    color: #fef8eb;
    overflow: hidden;
  }

  .webdev-final-cta__kicker {
    margin: 0 0 0.75rem;
    font-family: "Montserrat", sans-serif;
    font-size: 0.8rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: rgba(250, 218, 184, 0.95);
    font-weight: 700;
  }

  .webdev-final-cta__title {
    margin: 0;
    font-family: "Abril Fatface", serif;
    font-size: clamp(2rem, 3.8vw, 2.7rem);
    line-height: 1.08;
    color: #fef8eb;
  }

  .webdev-final-cta__text {
    margin: 1rem auto 0;
    max-width: 62ch;
    font-family: "Darker Grotesque", sans-serif;
    font-size: 1.25rem;
    line-height: 1.5;
    color: rgba(254, 248, 235, 0.88);
  }

  .webdev-final-cta__actions {
    margin-top: 1.75rem;
    display: flex;
    justify-content: center;
    gap: 0.75rem;
    flex-wrap: wrap;
  }

  .webdev-final-cta__btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.9rem 1.6rem;
    border-radius: 999px;
    text-decoration: none;
    font-family: "Montserrat", sans-serif;
    font-size: 0.85rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    transition:
      transform 0.15s ease,
      background-color 0.15s ease,
      border-color 0.15s ease,
      color 0.15s ease;
  }

  .webdev-final-cta__btn--primary {
    background: #fadab8;
    border: 1px solid #fadab8;
    color: #152705;
  }

  .webdev-final-cta__btn--primary:hover {
    background: #fef8eb;
    border-color: #fef8eb;
    transform: translateY(-1px);
  }

  .webdev-final-cta__btn--secondary {
    background: transparent;
    border: 1px solid rgba(250, 218, 184, 0.55);
    color: #fadab8;
  }

  .webdev-final-cta__btn--secondary:hover {
    background: rgba(250, 218, 184, 0.12);
    border-color: rgba(250, 218, 184, 0.8);
    transform: translateY(-1px);
  }

  .webdev-final-cta__btn:focus-visible {
    outline: 2px solid #fadab8;
    outline-offset: 3px;
  }
</style>
